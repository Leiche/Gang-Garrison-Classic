<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<event category="COLLISION" with="Character">
  <actions>
    <action id="603" library="1">
      <!--action name: Code-->
      <kind>CODE</kind>
      <allowRelative>false</allowRelative>
      <question>false</question>
      <canApplyTo>true</canApplyTo>
      <actionType>CODE</actionType>
      <functionName/>
      <relative>false</relative>
      <not>false</not>
      <appliesTo>.self</appliesTo>
      <arguments>
        <argument kind="STRING">        if(other.id != ownerPlayer.object and other.team != team  &amp;&amp; other.hp &gt; 0) {
            other.hp -= damage/4;
            other.lastDamageDealer = ownerPlayer;
            other.lastDamageSource = weapon;
            sticksTo = other.id;
            stickRelativeX = x-other.x;
            stickRelativeY = y-other.y;
            speed=0;
            var blood;
                with(other) {
                    if other.team != team {
                        //motion_add(other.direction, other.speed*0.03);
                        cloakAlpha = min(cloakAlpha + 0.1, 1);    
                        }
                    }
            } else if (other.team == team and other.hp &gt; 0) {
                other.hp += damage/2;
                sticksTo = other.id;
                stickRelativeX = x-other.x;
                stickRelativeY = y-other.y;
                speed=0;
                with (Flame) {
                if sticksTo != -1
                    if sticksTo == other.id
                    instance_destroy();
                }
                with (Gas) {
                if sticksTo != -1
                    if sticksTo == other.id
                    instance_destroy();
            }
        }
</argument>
      </arguments>
    </action>
  </actions>
</event>
